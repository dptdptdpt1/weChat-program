# 宝利足球赛事通 - 代码架构梳理

## 📋 项目概述

**项目名称**: 宝利足球赛事通  
**项目类型**: 微信小程序 + 后端API  
**主要功能**: 足球赛事资讯展示、用户管理、客服联系、轮播图展示

---

## 🏗️ 整体架构

```
weChat-program/
├── api/                    # 后端 FastAPI 服务
├── miniprogram/            # 前端 Taro 小程序
└── docs/                   # 部署文档
```

---

## 🎯 技术栈

### 后端 (api/)
- **框架**: FastAPI
- **ORM**: SQLAlchemy
- **数据库**: SQLite (文件数据库)
- **管理后台**: SQLAdmin
- **认证**: 微信小程序登录 (OAuth2)
- **部署**: Docker + Docker Compose

### 前端 (miniprogram/)
- **框架**: Taro 3.x
- **UI**: React + TypeScript
- **状态管理**: React Hooks
- **构建工具**: Webpack 5
- **Markdown解析**: Marked.js

---

## 📂 后端目录结构

```
api/
├── app/
│   ├── models/              # 数据模型
│   │   ├── event.py         # 赛事模型
│   │   ├── user.py          # 用户模型
│   │   ├── banner.py        # 轮播图模型
│   │   ├── customer_service.py  # 客服模型
│   │   └── uploaded_image.py   # 上传图片模型
│   │
│   ├── routes/              # API 路由
│   │   ├── events.py        # 赛事接口
│   │   ├── auth.py          # 认证接口
│   │   ├── banners.py       # 轮播图接口
│   │   ├── admin.py         # 后台管理接口
│   │   ├── upload.py        # 文件上传接口
│   │   ├── config.py        # 系统配置接口
│   │   └── health.py        # 健康检查
│   │
│   ├── services/            # 业务逻辑层
│   │   ├── event_service.py    # 赛事服务
│   │   ├── user_service.py     # 用户服务
│   │   └── customer_service.py # 客服服务
│   │
│   ├── schemas/             # Pydantic 数据验证模型
│   │   ├── event.py
│   │   ├── user.py
│   │   ├── customer_service.py
│   │   └── base.py
│   │
│   ├── utils/               # 工具类
│   │   ├── database.py      # 数据库配置
│   │   ├── error_handlers.py    # 异常处理
│   │   ├── exceptions.py    # 自定义异常
│   │   └── markdown_helper.py   # Markdown处理
│   │
│   └── admin.py             # 后台管理界面配置
│
├── data/                    # SQLite 数据库文件目录
│   └── football_events.db
│
├── uploads/                 # 上传文件存储目录
│   ├── events/              # 赛事图片
│   ├── thumbnails/          # 缩略图
│   ├── banners/             # 轮播图
│   └── customer-service/    # 客服二维码
│
├── main.py                  # FastAPI 应用入口
├── init_db.py               # 数据库初始化脚本
├── requirements.txt         # Python 依赖
├── Dockerfile               # Docker 镜像配置
├── docker-compose.yml       # Docker Compose 配置
└── .env                     # 环境变量配置
```

---

## 📂 前端目录结构

```
miniprogram/
├── src/
│   ├── pages/               # 页面
│   │   ├── index/           # 首页 - 赛事列表
│   │   ├── proposal/        # 今日方案页面
│   │   ├── profile/         # 个人中心
│   │   └── event-detail/    # 赛事详情
│   │
│   ├── components/          # 公共组件
│   │   ├── EventCard/       # 赛事卡片组件
│   │   ├── QRCodeModal/     # 二维码弹窗组件
│   │   └── SearchBar/       # 搜索栏组件
│   │
│   ├── services/            # API 服务层
│   │   ├── apiClient.ts     # HTTP 请求封装
│   │   ├── authService.ts  # 认证服务
│   │   ├── eventService.ts  # 赛事服务
│   │   ├── bannerService.ts # 轮播图服务
│   │   └── configService.ts # 配置服务
│   │
│   ├── types/               # TypeScript 类型定义
│   │   └── index.ts
│   │
│   ├── utils/               # 工具函数
│   │   ├── request.ts       # 请求工具
│   │   └── markdown.ts      # Markdown 解析
│   │
│   ├── app.ts               # 应用入口
│   ├── app.config.ts        # 应用配置
│   └── app.scss             # 全局样式
│
├── config/                  # 环境配置
│   ├── dev.ts               # 开发环境
│   ├── prod.ts              # 生产环境
│   └── index.ts             # 配置入口
│
├── dist/                    # 编译输出目录
├── package.json             # 项目依赖
├── tsconfig.json            # TypeScript 配置
└── project.config.json      # 微信开发者工具配置
```

---

## 🔄 核心业务流程

### 1. 用户登录流程

```typescript
// 前端流程 (miniprogram/src/services/authService.ts)
1. 获取用户授权信息 (wx.getUserProfile)
2. 调用微信登录 (wx.login) 获取 code
3. 发送 code + 用户信息到后端
4. 后端验证并返回用户信息
5. 存储到本地缓存
```

```python
# 后端流程 (api/app/services/user_service.py)
1. 接收 code + 用户信息
2. 使用 code 调用微信服务器获取 openid
3. 查询或创建用户记录
4. 返回用户信息
```

### 2. 赛事列表展示流程

```typescript
// 前端流程 (miniprogram/src/pages/index/index.tsx)
1. 页面加载 (useLoad)
2. 调用 eventService.getEvents()
3. 获取轮播图数据
4. 渲染 EventCard 列表
```

```python
# 后端流程 (api/app/services/event_service.py)
1. 接收分页参数 (page, page_size)
2. 查询数据库 (按日期降序)
3. 支持关键词搜索
4. 返回分页数据
```

### 3. 赛事详情展示流程

```typescript
// 前端流程 (miniprogram/src/pages/event-detail/index.tsx)
1. 接收赛事 ID (路由参数)
2. 调用 eventService.getEventDetail(id)
3. 增加浏览量 (eventService.increaseViewCount(id))
4. 解析 Markdown 内容
5. 渲染详情页
```

```python
# 后端流程 (api/app/services/event_service.py)
1. 根据 ID 查询赛事
2. 浏览量 +1
3. 返回详情 (包含 Markdown 内容)
```

---

## 🗄️ 数据库表结构

### events (赛事表)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| title | String(200) | 赛事标题 |
| date | Date | 赛事日期 |
| content | String(50000) | Markdown 内容 |
| cover_image | String(500) | 封面图 URL |
| view_count | Integer | 浏览量 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### users (用户表)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| open_id | String(100) | 微信 OpenID |
| nick_name | String(50) | 昵称 |
| avatar_url | String(500) | 头像 URL |
| created_at | DateTime | 创建时间 |
| last_login_at | DateTime | 最后登录时间 |

### banners (轮播图表)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| image_url | String(500) | 图片 URL |
| title | String(200) | 标题 |
| link_url | String(500) | 跳转链接 |
| sort_order | Integer | 排序 |
| is_active | Boolean | 是否启用 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### customer_services (客服配置表)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| qr_code_url | String(500) | 客服二维码 URL |
| online_time | String(100) | 在线时间 |
| updated_at | DateTime | 更新时间 |

---

## 🔌 API 接口列表

### 赛事相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/events` | 获取赛事列表 (支持分页、搜索) |
| GET | `/api/events/{id}` | 获取赛事详情 |
| POST | `/api/events/{id}/view` | 增加浏览量 |

### 认证相关

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/wx-login` | 微信登录 |
| PUT | `/api/auth/nickname` | 更新用户昵称 |

### 轮播图相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/banners` | 获取轮播图列表 |

### 系统配置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/config/customer-service` | 获取客服配置 |

### 文件上传

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload/event-image` | 上传赛事图片 |
| POST | `/api/upload/banner` | 上传轮播图 |
| POST | `/api/upload/customer-service` | 上传客服二维码 |

### 后台管理

| 方法 | 路径 | 说明 |
|------|------|------|
| ALL | `/admin` | SQLAdmin 管理后台 |

---

## 🎨 页面功能说明

### 1. 首页 (pages/index)

**功能**:
- 显示顶部轮播图
- 显示赛事列表 (EventCard 组件)
- 下拉刷新
- 加载状态/错误状态/空状态处理

**关键文件**:
- `miniprogram/src/pages/index/index.tsx` - 页面逻辑
- `miniprogram/src/components/EventCard/index.tsx` - 赛事卡片组件

### 2. 今日方案 (pages/proposal)

**功能**:
- 展示今日赛事推荐方案
- 搜索功能

**关键文件**:
- `miniprogram/src/pages/proposal/index.tsx`
- `miniprogram/src/components/SearchBar/index.tsx` - 搜索栏组件

### 3. 赛事详情 (pages/event-detail)

**功能**:
- 显示赛事标题、日期
- 解析并渲染 Markdown 内容
- 显示浏览量
- 图片懒加载

**关键文件**:
- `miniprogram/src/pages/event-detail/index.tsx`
- `miniprogram/src/utils/markdown.ts` - Markdown 解析工具

### 4. 个人中心 (pages/profile)

**功能**:
- 用户头像、昵称展示
- 微信授权登录
- 修改昵称
- 显示客服二维码
- 客服联系功能

**关键文件**:
- `miniprogram/src/pages/profile/index.tsx`
- `miniprogram/src/components/QRCodeModal/index.tsx` - 二维码弹窗组件

---

## 🔧 配置文件说明

### 后端配置

**.env**
```env
WECHAT_APP_ID=你的AppID
WECHAT_APP_SECRET=你的AppSecret
```

### 前端配置

**config/dev.ts** - 开发环境
```typescript
export default {
  defineConstants: {
    'process.env.API_BASE_URL': '"http://localhost:8000"'
  }
}
```

**config/prod.ts** - 生产环境
```typescript
export default {
  defineConstants: {
    'process.env.API_BASE_URL': '"https://你的后端域名.com"'
  }
}
```

**app.config.ts** - 小程序配置
```typescript
{
  pages: ['pages/index/index', ...],  // 页面路径
  window: { ... },                    // 窗口样式
  tabBar: { ... }                     // 底部导航
}
```

---

## 🚀 部署流程

### 后端部署

1. 上传代码到服务器
2. 配置 `.env` (AppID、AppSecret)
3. 启动 Docker 容器: `docker-compose up -d`
4. 初始化数据库: `docker exec -it football-api python init_db.py`
5. 配置 Nginx + HTTPS
6. 访问 `/docs` 和 `/admin` 验证

### 前端部署

1. 修改 `config/prod.ts` 中的 API 地址
2. 安装依赖: `pnpm install`
3. 构建生产版本: `pnpm run build:weapp`
4. 使用微信开发者工具上传
5. 提交审核并发布

---

## 📊 项目特点

### 优点
- ✅ 前后端分离架构
- ✅ 完整的 TypeScript 类型定义
- ✅ Markdown 富文本支持
- ✅ 微信登录集成
- ✅ Docker 容器化部署
- ✅ SQLAdmin 后台管理
- ✅ 组件化开发
- ✅ 状态管理清晰

### 技术亮点
- 使用 FastAPI 异步框架，性能优秀
- ORM 使用 SQLAlchemy，数据库操作方便
- 前端使用 Taro，可编译到多端
- 自定义 Markdown 渲染，支持图片和格式化
- 完整的错误处理和日志记录

---

## 📝 关键文件说明

### 后端核心文件

| 文件 | 说明 |
|------|------|
| `main.py` | FastAPI 应用入口，路由注册、中间件配置 |
| `init_db.py` | 数据库初始化脚本 |
| `app/models/event.py` | 赛事数据模型 |
| `app/services/event_service.py` | 赛事业务逻辑 |
| `app/routes/events.py` | 赛事 API 路由 |
| `app/utils/database.py` | 数据库连接配置 |

### 前端核心文件

| 文件 | 说明 |
|------|------|
| `src/app.ts` | 小程序入口 |
| `src/app.config.ts` | 全局配置 (页面、TabBar) |
| `src/services/apiClient.ts` | HTTP 请求封装 |
| `src/pages/index/index.tsx` | 首页逻辑 |
| `src/utils/request.ts` | 请求工具函数 |

---

## 🔐 安全要点

1. **微信登录**: 使用官方 OAuth2 流程，code 换取 openid
2. **CORS**: 生产环境需配置具体域名
3. **HTTPS**: 生产环境必须使用 HTTPS
4. **环境变量**: 敏感信息存储在 `.env` 文件
5. **数据库**: SQLite 文件权限控制
6. **文件上传**: 限制文件类型和大小

---

## 📈 扩展建议

### 功能扩展
- [ ] 用户收藏赛事功能
- [ ] 赛事评论功能
- [ ] 消息通知功能
- [ ] 分享功能
- [ ] 数据统计

### 技术优化
- [ ] 添加 Redis 缓存
- [ ] 图片 CDN 加速
- [ ] 前端代码分割
- [ ] 接口限流
- [ ] 日志系统优化

---

## 🎓 学习要点

### 后端开发
- FastAPI 异步框架使用
- SQLAlchemy ORM 操作
- Pydantic 数据验证
- Docker 容器化部署
- 微信小程序登录流程

### 前端开发
- Taro 跨端框架
- React Hooks 状态管理
- TypeScript 类型定义
- Markdown 解析渲染
- 小程序生命周期
- 组件化开发

---

## 📞 联系方式

如有问题，请查看:
- 后端部署文档: `后端部署文档.md`
- 前端部署文档: `前端小程序部署文档.md`
- 后台管理: https://你的域名/admin
- API 文档: https://你的域名/docs

---

**生成时间**: 2025-12-31
